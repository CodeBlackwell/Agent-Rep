[project]
name = "showmeoff"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "neo4j",
    "gradio",
    "openai",
    "python-dotenv",
    "tree-sitter",
    "pyvis",
    "pytest",
    "tqdm",
]
